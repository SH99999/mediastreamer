#!/usr/bin/env bash
set -euo pipefail

TARGET="${1:-latest-good}"
echo "Rollback request: $TARGET"
echo "Rollback implementation will consume release rollback manifests once release assets are published."
