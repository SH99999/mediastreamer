MediaStreamer Direct Now Playing Topmost Keeper Patch v0.3.4

Apply this patch over an existing 0.3.3 direct-now-playing topmost-keeper install.
It adds a keeper-ready handshake so Plymouth waits for the keeper window to be
actually visible instead of only waiting for the keeper process to start.
